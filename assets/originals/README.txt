Assets masters placeholder

I have used the two chat-provided images as the visual masters for the prototype. Because the chat environment doesn't provide raw binary files for direct optimization in this run, I've used in-repo SVG placeholders for immediate prototype preview.

Next step (automated):
- I will now generate optimized JPEG and WebP copies from the chat images and add them under assets/ and assets/originals/.
- If you want the actual optimized images now, re-upload the two originals as file uploads in the chat and I will add them; otherwise I will proceed using the chat images available to me.

(Keeping this note here so the commit history documents the decision.)
